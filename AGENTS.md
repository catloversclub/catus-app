# Project Conventions

- Prefer NativeWind `className` for static React Native styling.
- Use inline `style` only for runtime-calculated values, platform APIs, or values that cannot be expressed clearly with existing utility classes.
- Write commit messages with the existing prefix convention and a Korean summary, for example `feat: 한국어 요약`.
